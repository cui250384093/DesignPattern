package com.yl.strategy;

/**
 * @author candk
 * @Description
 * @date 5/6/21 - 2:16 PM
 */
public class StrategyPattern {
    /**
     * 策略模式（Strategy Pattern）中，定义算法族，分别封装起来，让他们之间可以互相替换，次模式让算法的变化独立与使用算法的客户
     * 该模式体现了几个设计原则：
     * 1. 把变化的代码从不变的代码中分离出来
     * 2. 针对接口编程而不是具体类（定义了策略接口）
     * 3. 采用组合/聚合，少用继承
     */
}
